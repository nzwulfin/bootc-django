---
# defaults file for django
db_host: "{{groups['db-hosts'] | first}}"
db_name: djangoapp
db_user: djangouser
db_password: qwer4321
project_dir: django-project
django_dir: "/var/srv/{{ project_dir }}"
venv_dir: "{{ django_dir }}/django-venv"
